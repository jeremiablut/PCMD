import os
from operator import truediv

os.rmdir("PPCMD")
#some random stuff to look like this is very complicated
Allow_MISSION = "true"
delete_PP_files = "activate"
if Allow_MISSION != "true" or delete_PP_files != "not_activate":
    def run_delete(Null):
        print("DONE_FLOAT")

exit()